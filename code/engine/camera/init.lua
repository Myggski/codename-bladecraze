--[[
  This is just a silly thing to get prettier require urls
  Instead of local camera = require "code.engine.camera.camera"
  You get local camera = require "code.engine.camera"
]]

local camera = require "code.engine.camera.camera"

return camera
